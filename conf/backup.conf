#
#-----------------------------------------------------------------------------------------
# SMARTI - Backup Retention Configuration
#-----------------------------------------------------------------------------------------
# Defines how many backup versions to retain before cleanup
# Automatic cleanup occurs during version updates
# Minimum: 2 (current + previous version)
# Usage: source /opt/smarti/conf/backup.conf
#-----------------------------------------------------------------------------------------

BACKUP_RETENTION=2
