#
#-----------------------------------------------------------------------------------------
# SMARTI - Dependencies Configuration
#-----------------------------------------------------------------------------------------
# Lists required packages/commands for each script and library
# Format: COMPONENT_NAME="space-separated list of dependencies"
# Used by dep_installer.lib.sh to verify and install missing packages
# Usage: source /opt/smarti/conf/dep.conf
#-----------------------------------------------------------------------------------------

#
# List DEPs - Scripts
SMARTI="bash"
INSTALL="bash git curl wget tar"
UNINSTALL="bash"
#
# List LIBs - Libraries
SHOW_BANNER_SMARTI="bash tput"
DETECT_LANGUAGE="bash"
ENSURE_SUDO="bash sudo"
SHOW_UPDATE_BANNER="bash"
DETECT_DISTRO="bash"
DEP_INSTALLER="bash"
MAKE_LINK="bash sudo"
CHECK_UPDATE="bash curl wget"
UPGRADE_SMARTI="bash git zstd tar"
BKP_FULL_VERSION="bash zstd tar"
ROLLBACK_SMARTI="bash zstd tar"
CHECK_INTERNET="bash ping curl wget"
REMOVE_LINK="bash sudo"
LOG="bash date"
ARGS="bash"
FS="bash mktemp"
UI="bash tput"
